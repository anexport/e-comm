import React from 'react';

const Cancel = () => {
    return (
        <div>
            <h1>Payment Canceled</h1>
            <p>It looks like you canceled the payment process. No worries, your order has not been processed.</p>
            <p>You can go back to your cart and try again.</p>
        </div>
    );
};

export default Cancel;
